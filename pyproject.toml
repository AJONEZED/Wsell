[project]
name = "wsell"
version = "0.1.0"
description = "Wholesale marketplace demand-aggregation and fulfillment algorithm"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
